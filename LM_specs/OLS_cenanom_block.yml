NAME: "ols-cenanom-block"
RHS_FORMULA: "AD + scale(current_scan_age) + SEX + NIVETUD + scale(`total intracranial`) + CEN_ANOM"
GROUPS: null # If groups is not null, a mixed GLM is computed
BY_BLOCK: True