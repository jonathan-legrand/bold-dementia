# COMPARE MAPS DEFAULT RUN
confounds_strategy:
  strategy: ["high_pass", "motion", "wm_csf", "global_signal"]
SOFT: False
ATLAS: "gillig-regions"
CLEAN_SIGNAL: True
MACRO_LABELS: True
NAME: "linear"
BALANCE_STRAT: []
LONGITUDINAL: False
RHS_FORMULA: "AD + scale(current_scan_age) + SEX + NIVETUD + scale(`total intracranial`)"
GROUPS: null # If groups is not null, a mixed GLM is computed