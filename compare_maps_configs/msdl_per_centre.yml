# COMPARE MAPS DEFAULT RUN
confounds_strategy:
  strategy: ["high_pass", "motion", "wm_csf", "global_signal"]
SOFT: False
ATLAS: "msdl"
CLEAN_SIGNAL: True
MACRO_LABELS: True
NAME: "global-signal-mixed-centre"
BALANCE_STRAT: []
LONGITUDINAL: False
RHS_FORMULA: "AD + scale(current_scan_age) + SEX + NIVETUD"
GROUPS: "CEN_ANOM" # If groups is not null, a mixed GLM is computed